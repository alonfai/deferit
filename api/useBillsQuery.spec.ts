describe('api', () => {
  describe('fetchBills', () => {
    test('check for valid API_URL in the environment variable', async () => {});

    test('check for network error', async () => {});
  });
});
