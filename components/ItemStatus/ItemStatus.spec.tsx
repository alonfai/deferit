// import { render, fireEvent } from '@testing-library/react-native';
// import ItemSummary from './ItemSummary';

describe('<ItemStatus />', () => {
  it('demo', () => {
    expect(1).toEqual(1);
  });
});
