// import { render, fireEvent } from '@testing-library/react-native';
// import ItemPopup from './ItemPopup';

describe('<ItemPopup />', () => {
  it('demo', () => {
    expect(1).toEqual(1);
  });
});
